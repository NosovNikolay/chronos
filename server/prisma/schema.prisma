generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model User {
  id    String @db.Uuid @id @default(uuid())
  login String @unique @db.VarChar(50)
  UserCreateArgs["login"]
}
